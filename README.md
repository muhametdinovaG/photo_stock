# Микросервис экспорта для сервисов стоковых изображений

## Версии
Версия Python: разрабатывалось на 3.6.8.

## Развертка рабочего проекта
Из папки с проектом билдим:
`docker build --tag=stockupload .`

Далее запускаем:
`docker run -d stockupload`
